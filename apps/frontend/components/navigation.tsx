'use client';
import {CalendarDays,ChartNoAxesCombined,Dumbbell,House,UserRound} from 'lucide-react';
import Link from 'next/link';import {usePathname} from 'next/navigation';
const items=[['/dashboard','Beranda',House],['/schedule','Jadwal',CalendarDays],['/workout/today','Latihan',Dumbbell],['/progress','Progres',ChartNoAxesCombined],['/profile','Profil',UserRound]] as const;
export function BottomNav(){const path=usePathname();return <nav className="fixed inset-x-0 bottom-0 z-40 mx-auto grid max-w-xl grid-cols-5 border-t border-line bg-card/95 px-2 pb-[env(safe-area-inset-bottom)] backdrop-blur">{items.map(([href,label,Icon],i)=><Link key={href} href={href} className={`relative grid min-h-16 place-items-center gap-0.5 text-[11px] ${path.startsWith(href.split('/')[1]?`/${href.split('/')[1]}`:href)?'text-green':'text-slate-400'} ${i===2?'-mt-4':''}`}><span className={i===2?'grid size-14 place-items-center rounded-full bg-green text-black shadow-lg shadow-green/20':''}><Icon size={i===2?26:21}/></span>{label}</Link>)}</nav>}
export function AppShell({title,children,action}:{title:string,children:React.ReactNode,action?:React.ReactNode}){return <><main className="container-app safe-bottom"><header className="mb-6 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-green">CutTrack</p><h1 className="font-heading text-2xl font-bold">{title}</h1></div>{action}</header>{children}</main><BottomNav/></>}
