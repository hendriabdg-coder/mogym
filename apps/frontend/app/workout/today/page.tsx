'use client';import{useEffect}from'react';import{useRouter}from'next/navigation';import{api}from'@/lib/api';export default function Today(){const r=useRouter();useEffect(()=>{api<any>('/sessions/today').then(x=>r.replace(x.restDay?'/dashboard':`/workout/${x.id}`)).catch(()=>r.replace('/dashboard'))},[r]);return <main className="grid min-h-screen place-items-center text-slate-400">Mencari sesi hari ini…</main>}
