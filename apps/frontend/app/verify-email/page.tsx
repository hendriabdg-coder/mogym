'use client';import {useSearchParams}from'next/navigation';import {Suspense,useEffect,useState}from'react';import Link from'next/link';import{api}from'@/lib/api';function Verify(){const p=useSearchParams(),[status,setStatus]=useState(p.get('token')?'Memverifikasi…':'Periksa kotak masukmu untuk tautan verifikasi.');useEffect(()=>{const t=p.get('token');if(t)api(`/auth/verify-email?token=${t}`).then(()=>setStatus('Email berhasil diverifikasi.')).catch((e)=>setStatus(e.message))},[p]);return <main className="container-app grid min-h-screen place-items-center"><div className="glass max-w-md rounded-3xl p-8 text-center"><h1 className="font-heading text-2xl font-bold">Verifikasi email</h1><p className="my-5 text-slate-400">{status}</p><Link className="font-bold text-green" href="/login">Kembali ke login</Link></div></main>}export default function Page(){return <Suspense><Verify/></Suspense>}
